# Python dependencies for the LOCAL/DEV ingestion + upload pipeline.
# The deployed Next.js app (Vercel) consumes the pre-built data/claims.json and
# does NOT require Python at runtime. Install these only to run scripts/ locally.
pypdf>=4.0
httpx>=0.27
sentence-transformers>=3.0
